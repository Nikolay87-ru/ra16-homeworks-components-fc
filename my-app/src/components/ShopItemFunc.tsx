import React from 'react';
import { ShopItem } from './types'

interface ShopItemProps {
  item: ShopItem;
}

const ShopItemFunc: React.FC<ShopItemProps> = ({ item }) => {
  
};

export default ShopItemFunc;